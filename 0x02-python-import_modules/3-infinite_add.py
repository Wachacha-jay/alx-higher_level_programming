#!/usr/bin/python3
if __name__ == "__name__":
    import sys
    result = 0
    if (len(sys.argv) > 1):
        for i in range (1, len(sys.argv)):
            sum += (int(sys.argv[i]))
    print("{}" .format(result))
